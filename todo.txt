Tags:
all tags must be at the start of a new line

COLOR NAME R G B
KEY NAME KEY_CODE COLOR
COMMENT ANYTHING
LAYER NAME LAYOUT_MACRO ...KEY
INJECT path

todo
add in multi press layers
update macros so that they can refference keys already defined (???)
consider changing the LAYOUT macros to width height vals and auto generating the macro define
set it up to run with QMK so it can make the .bin right away and delete the keymap files